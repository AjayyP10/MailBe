{
  "name": "mailbe-backend",
  "version": "0.1.0",
  "description": "FastAPI backend for MailBe - AI-powered email client",
  "main": "src/main.py",
  "type": "module",
  "scripts": {
    "dev": "uvicorn src.main:app --reload --host 0.0.0.0 --port 8000",
    "start": "uvicorn src.main:app --host 0.0.0.0 --port 8000",
    "migrate": "alembic upgrade head",
    "test": "pytest tests/"
  }
}
